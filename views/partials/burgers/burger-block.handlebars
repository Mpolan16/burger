<li>
	{{burger_name}}

	<button class="change-devoure" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}ALL EATEN!{{else}}EATING TIME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>